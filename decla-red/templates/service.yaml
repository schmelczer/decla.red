apiVersion: v1
kind: Service
metadata:
  name: {{ include "decla-red.fullname" . }}
  labels:
    {{- include "decla-red.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "decla-red.selectorLabels" . | nindent 4 }}
