apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "decla-red.fullname" . }}-test-connection"
  labels:
    {{- include "decla-red.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "decla-red.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
