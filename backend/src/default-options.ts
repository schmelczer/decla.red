import { Options } from './options';

export const defaultOptions: Options = {
  port: 3000,
  name: 'Test server',
  playerLimit: 8,
  npcCount: 4,
  seed: 0,
  scoreLimit: 500,
  worldSize: 8000,
};
